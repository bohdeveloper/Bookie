/* let like = false;
function color(item) {

	switch (item.id) {
		case "red":
			switcherRed(item);
			break;
		case "green":
			switcherGreen(item);
			break;
	}
}

function switcherRed(item) {
	switch (like) {
		case false:
			item.classList.add("text-danger");
			item.classList.remove("text-secondary");
			like = true;
			break;
		case true:
			item.classList.add("text-secondary");
			item.classList.remove("text-danger");
			like = false;
			break;
		default:
			break;
	}
}

function switcherGreen(item) {
	switch (like) {
		case false:
			item.classList.add("text-success");
			item.classList.remove("text-secondary");
			like = true;
			break;
		case true:
			item.classList.add("text-secondary");
			item.classList.remove("text-success");
			like = false;
			break;
		default:
			break;
	}
} */
